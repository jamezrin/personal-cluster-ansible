---
cluster:
  vars:
    default_if_name: enp0s3

  children:
    masters:
      hosts:
        ocvm-a1-1:
          # As an example:
          # default_if_name: eth0

    workers:
      hosts:
        ocvm-a1-2:
