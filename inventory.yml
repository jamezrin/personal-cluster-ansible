---
cluster:
  vars:
    default_if_name: enp0s3
    ansible_python_interpreter: auto_silent
  children:
    servers:
      hosts:
        ocvm-a1-1:
          # As an example:
          # default_if_name: eth0

    agents:
      hosts:
        ocvm-a1-2:
